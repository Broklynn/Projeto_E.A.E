
/*function gerarDados() {
    const umidadeSolo = (Math.random() * 100).toFixed(2);
    const umidadeAr = (Math.random() * 100).toFixed(2);
    const temperaturaAgua = (Math.random() * 30 + 10).toFixed(2);


    document.getElementById('umidade-solo').textContent = `${umidadeSolo} %`;
    document.getElementById('umidade-ar').textContent = `${umidadeAr} %`;
    document.getElementById('temperatura-agua').textContent = `${temperaturaAgua} °C`;
}
setInterval(gerarDados, 5000);

gerarDados();*/